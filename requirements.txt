# --- ACC Control Plane -------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
python-multipart>=0.0.12

# --- Agents (Google ADK + Gemini) -------------------------------------------
google-adk>=1.0.0
google-genai>=1.0.0

# --- Google Cloud ------------------------------------------------------------
google-cloud-firestore>=2.19
google-cloud-pubsub>=2.26
google-cloud-secret-manager>=2.21
google-cloud-modelarmor>=0.1.0

# --- Observability -----------------------------------------------------------
opentelemetry-api>=1.28
opentelemetry-sdk>=1.28
# Optionnel : les spans HTTP automatiques. Les versions anterieures a
# 0.58b0 sont incompatibles avec FastAPI >= 0.141 (objets _IncludedRouter).
# ACC sonde la compatibilite au demarrage et se degrade proprement.
# opentelemetry-instrumentation-fastapi: removed on purpose (ADR-052).
# Versions below 0.65b0 crash on FastAPI >= 0.141 and pip backtracks to
# them inside the container, constrained by google-adk.
opentelemetry-exporter-otlp-proto-grpc>=1.28
opentelemetry-exporter-gcp-trace>=1.7.0

# --- Tests -------------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24
